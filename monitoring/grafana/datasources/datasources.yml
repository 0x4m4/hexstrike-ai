apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true

  - name: PostgreSQL
    type: postgres
    url: postgres:5432
    database: hexstrike
    user: hexstrike
    secureJsonData:
      password: hexstrike_secure_pass_2024
    jsonData:
      sslmode: disable